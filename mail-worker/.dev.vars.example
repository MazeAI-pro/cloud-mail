# 复制为 .dev.vars 后按需填写（.dev.vars 已 gitignore）
# 本地 wrangler dev 专用；wrangler deploy 不会上传此文件
jwt_secret=
feishu_switch=true
feishu_app_id=
feishu_redirect_uri=http://localhost:3001/login
feishu_app_secret=
# 逗号分隔；启用飞书时必填，与飞书应用返回的 tenant_key 一致
FEISHU_ALLOWED_TENANT_KEYS=
